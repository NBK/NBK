#pragma once

// TODO: manages and draws every entity in the game. "static" entities should be put into a tree of some sort!

class CEntityManager
{
public:
	CEntityManager(void);
	~CEntityManager(void);
};
